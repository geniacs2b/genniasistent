# --- STRIPE CONFIGURATION ---
# Obtener de https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Obtener tras configurar el webhook en Stripe Dashboard (redirigir a /api/stripe/webhook)
# O mediante Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
STRIPE_WEBHOOK_SECRET="whsec_..."

# IDs de Precios (Product Prices) creados en Stripe
# Nota: Debes agregar metadata { "quota": "500" } etc. a los Productos en Stripe
STRIPE_PRICE_STARTER="price_..."
STRIPE_PRICE_PRO="price_..."
STRIPE_PRICE_ENTERPRISE="price_..."

# URLs de Redirección (Usar el dominio real en Vercel o LOCAL para pruebas)
# Ejemplo Vercel: https://genniasistent.vercel.app/app/billing?success=true
STRIPE_SUCCESS_URL="https://genniasistent.vercel.app/app/billing?success=true"
STRIPE_CANCEL_URL="https://genniasistent.vercel.app/app/billing?canceled=true"
